[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  framework = "astro"
  targetPort = 3000

[functions]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"